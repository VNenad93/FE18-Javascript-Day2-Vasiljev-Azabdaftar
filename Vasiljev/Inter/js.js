// capitalize the string

const capitalize = string => string.charAt(0).toUpperCase() + string.slice(1)

// average grade

function average(math, physics, english) {
    return avg = (math + physics + english) / 3
}

console.log(average(3, 4, 5))